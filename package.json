{
  "name": "share",
  "version": "1.0.0",
  "private": true,
  "description": "REELDROP — Retro Modern Movie Archive (Vanilla HTML/CSS/JS)",
  "scripts": {
    "build": "echo 'Static build ready'",
    "start": "serve . -p 8080",
    "dev": "serve . -p 8080"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "sharp": "^0.35.3"
  }
}
